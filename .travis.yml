---
language: python
python: "2.7"
before_install:
 - yum upgrade -y
install:
  - yum install ansible
  - ansible-galaxy install geerlingguy.apache
notifications:
  webhooks: https://galaxy.ansible.com/api/v1/notifications/
